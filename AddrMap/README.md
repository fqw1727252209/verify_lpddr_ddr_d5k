# AddrMap 地址映射测试

本目录用于存放地址映射相关的验证用例和脚本。

## 测试范围

- 验证控制器地址到 DRAM 物理地址（Row / Bank / Column）的映射逻辑
- 不同地址交织模式的正确性验证
- 边界地址映射行为验证

## 目录结构建议

```
AddrMap/
├── tc/      # 测试用例
├── seq/     # 测试序列
├── ref/     # 参考文档
└── README.md
```
