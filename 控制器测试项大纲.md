# 控制器测试项大纲 (Controller Test Items Outline)

本文档专门用于汇总和整理从 IP 数据手册以及验证计划中提取的各项验证测试点（Test Items）。

---

## 1. 地址映射 (Address Mapping)

| 测试大项 | Level 1 | Level 2 | Level 3 | 场景描述 | 测试方法及配置说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **地址映射功能测试** |  |  |  |  |  |
|  | **自定义地址映射功能** |  |  |  |  |
|  |  | **不截位离散地址到连续地址映射功能** | | 检查不截位离散地址到连续地址的映射的正确定向 | **期望结果**：发送离散的地址请求，能正确映射为一段连续地址，访问正确。<br><br>**测试方法**：配置 `dmu_reg_addr_region_0/1/2/3_ch0/1` 寄存器进行空间配置，目前是两段空间 region0: 48'h8000_0000 - 44'hffff_ffff (2~4GB), region1: 8TB - 8TB+62GB;<br>`dmu_reg_addr_strip_bits_hi/lo_ch0/ch1` 配置为 0（不截位），初始化完成后，发送写后读测试激励。 |
| | | **截位离散地址到连续地址映射功能** | | 检查截位离散地址到连续地址的映射的正确定向 | **期望结果**：发送离散的地址请求，截位正确，能正确映射为一段连续地址，访问正确。<br><br>**测试方法**：空间配置同上；`dmu_reg_addr_strip_bits_hi/lo_ch0/ch1` 配置为任意值（实际应用中不能随意截取），初始化完成后，发送写后读测试激励，判断地址映射正确性。 |
| | **控制器连续性地址到DRAM接口颗粒地址映射** | **默认配置** | | 初始化采用的默认映射方式：<br>`row[15:0] -> cid[2:0] -> cs -> ba[1:0] -> bg[2:0] -> col[5:0]` | **期望结果**：数据访问正确。<br><br>**测试方法**：根据默认映射关系配置各位置寄存器（例如 `Col0Pos=3, Col1Pos=4... Row0Pos=16... Ba0Pos=9...` 等）；配置完成做数据访问。 |
| | | **随机配置** | | 在满足模型位宽的情况下，配置随机映射 | **期望结果**：数据访问正确。<br><br>**测试方法**：配置映射：合理情况下做随机配置；配置完成做数据访问。 |
| | | **性能较优** | | 采用符合最佳性能需求的映射配置 | **期望结果**：数据访问正确。<br><br>**测试方法**：具体寄存器值参见性能推荐配置；配置完成做数据访问。 |

---

## 2. CTL BIST 测试 (Built-In Self Test)

| 测试大项 | Level 1 | Level 2 / 模式 | Level 3 | 场景描述 | 测试方法及配置说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **CTL BIST 测试** |  |  |  |  |  |
|  | **CTL BIST 常规测试** |  |  |  |  |
|  |  | **地址检测** | | 自定义开/关 ecc 都会进行该测试 | **测试方法**: <br>1. 配置 XmuHold 为 1，停止接收命令。<br>2. 配置 BistMode 为 0，配置 `cdrBistAddrCheckBits` 为需检测的地址 bit；关闭 SrEn, PdnEn, CtrlupdEn, PhymstrEn；<br>3. 读 BistReady 直到为 1，使能 BistEn；<br>4. 检测到 ErrState[9] 为 1 时读取 BistTestResult；<br>5. 若 Result 为 1(Pass)，关闭 Bist，恢复配置，释放 XmuHold；<br>6. 若为 0，错误地址及数据记录在 BistErrAddr/BistExpData 等对应寄存器，随后关闭 Bist 并恢复原状。 |
|  |  | **数据检测** | |  |  |
| | |  | **MOVI1 3N 算法 CTL BIST 数据检查功能** | 使用 MOVI1 3N 算法对数据进行检测 | **测试方法**: <br>1. 停止接收命令(XmuHold=1)；<br>2. 配置 BistMode 为 1，配置起始与结束地址(BistStartAddr/BistEndAddr)，关闭低功耗及总线控制等配置；<br>3. 读 BistReady=1 后使能 BistEn；<br>4. 等待 ErrState[9]=1 后判定 BistTestResult，结果为 Pass 或记录错误并恢复配置释放系统。 |
| | | | **Limited MOVI 算法 CTL BIST 数据检查功能** | 使用 Limited MOVI 算法对数据进行检测 | **测试方法**: <br>相比 MOVI1 3N (BistMode=1)，需**配置 BistMode 为 2**。<br>同时需配置错误容忍下限 `BistErrStop`，若要测 ECC 附带验证，打开 `BistSbEccCheckEn` 并配置相应 Pattern，其余准备/结束步骤同上。 |
| | | | **Self-Refresh Retention 测试 CTL BIST 数据检查功能** | BIST 模式为自刷新保留测试时的检查 | **测试方法**: <br>1. 配置 **BistMode 为 4** 进行准备与开启使能；<br>2. **关键步骤**: 读 `BistInRetWait` 直到为 1，随后写 `BistRetExit` 为 1 进行唤醒等待；<br>3. 检测 ErrState[9]=1，获取最终的保留测试验证结果。 |
| | | | **Idle Retention 测试 CTL BIST 数据检查功能** | BIST 模式为空闲保留测试时的检查 | **测试方法**: <br>**配置 BistMode 为 5**；<br>其余控制保留(retention)退出与等待的交互流程与 BistMode 4 类似，获取最终的数据静态保存能力验证结果。 |
| | **初始化写编程模式** | **开启 sbecc 下的初始化写模式 (DDR5_UDIMM/RDIMM)** | | BIST 模式为内存初始化编程模式时，仅开启 sbeccen 下的检查 | **测试方法**: <br>1. **配置 BistMode 为 3**，配置检测地址范围，开启 sbeccen。<br>2. 等待 ErrState[9]=1 后关闭 Bist 使能，释放 XmuHold。<br>3. BIST 结束后，对初始化的地址进行主机发起的实际 Read 操作，判断读出数据与写入(初始化)数据是否一致。 |
| | **CTL BIST 注错测试** | **MOVI 3N 算法注错** | | 对 MOVI 3N 算法注错，第一次失败即刻结束 | **测试方法**: <br>1. 使用 VIP/Model 的注错功能注错；配置 BistMode=1 与相关起止地址，配置 `BistErrStop` 定界。<br>2. 结果期望：Result=0 且第一次失败即停止报错，错误仅记录最开头一次后结束。 |
| | | **Limited MOVI / Retntion 验证注错** | **错误数量小于 BistErrStop** | 错误数达不到停止阈值，将坚持跑完所有地址 | **测试方法**: <br>配置相应的 BistMode 及定好 `BistErrStop`；验证测试会持续到遍历完最后一个地址才跳出。 |
| | | | **错误数量大于等于 BistErrStop** | 设定的停止条件触发后及时停止 | **测试方法**: <br>注错使得触发频次超出阈值，验证报错记录后是否直接跳出（Bist 提前结束扫描）。 |
| | | | **BistErrStop 设为 0** | 配置为0表示不设限，会运行完整全量地址 | **测试方法**: <br>配置 `BistErrStop=0`，一直跑到全地址扫描完，记录所有报错或最后的数据覆盖情况。 |
| | **bisteccchecken 与 ecc 使能组合验证** | **sbeccen 使能开启** | **注错 1bit** | 使用 VIP 注入 1bit 错误，校验 `bisteccchecken` 开/关时的机制 | **期望结果**：<br>1. `bisteccchecken` = On：ECC 会报“不可纠”和可纠中断并计数，BIST 也会自计数据错误且报错，ECC 实际被配置为测试 Pattern 内容。<br>2. `bisteccchecken` = Off：ECC 报错计数后 1bit 被纠正，因此 BIST 检查不发生错误(完全成功掩盖)，ECC 根据正常写请求生成。 |
| | | | **注错 2bit** | 注入 2bit 错误，校验二者功能组合预期 | **期望结果**：<br>1. `bisteccchecken` = On：ECC 报不可纠中断并计数，BIST 统计出错误。<br>2. `bisteccchecken` = Off：ECC 检测 2bit 错不可纠正，从而 BIST 拿回的数据依然是错的，BIST 也会报错。 |
| | | | **不注错** | 正常跑满配置，排查 BIST 自己覆盖 ECC 位导致的问题 | **期望结果**：<br>1. `bisteccchecken` = On：依然会强制校验并触发 ECC 异常统计。<br>2. = Off：双端正常无任何报错中断。 |
| | | **sbeccen 使能关闭** | **注错 1bit** | 未开全数据通路的 ECC 功能注错组合测试 | **期望结果**：<br>`bisteccchecken` = On或Off：ECC 都不会计录并中止，但 BIST 的比对中会计数到有数据出错了（结果为错），且 ECC 不会向颗粒真正写入纠错码。 |
| | | | **注错 2bit/不注错** | 参考使能开启的情况，关闭状态下无法纠这2bit或无错 | **期望结果**：<br>按未使能ECC的机制响应，BIST 根据数据是否一致自行报错。 |
| | **BIST 配置错误测试** | **越界与无效配置检测** | **地址 Start > End** | 故意配置起始终点倒置 | **期望结果**：配置异常指示信号 `BistCfgErrInfo[0]` 被拉高。 |
| | | | **BistADDRCheckBits = 0** | 开启了地址检测但检测范围为 0 | **期望结果**：配置异常指示信号 `BistCfgErrInfo[1]` 被拉高。 |
| | | | **BistMode 为非法模式** | 设置为未定义的 Bist 模式值 | **期望结果**：配置异常指示信号 `BistCfgErrInfo[2]` 被拉高。 |
| | **不同 rank 的 bist 测试** | | | 测试不同 rank 均支持 bist 测试并可以注错 | **测试方法**：用 chip-select 遍历每个 rank 使能 BIST 并注错。<br>**期望结果**：错误正常检测，故障地址寄存器成功捕捉第一个出错的地址。 |
| | **手动暂停 bist 流程测试** | | **手动暂停后重新使能** | 测试工作一段后主动挂起，再次恢复是否正确 | **测试方法**：打开 BIST 跑一段时间 -> 关闭使能(挂起) -> 再打开使能(恢复)。最后统计结果是否 pass。 |
| | | | **手动暂停后正常读写** | 测试工作一段后主动挂起，中间插主线业务读写 | **测试方法**： BIST 挂起后 -> 直接进行主机维度的正常写/读测试评估系统总线是否正常工作。 |

---

## 3. MR 功能测试 (Mode Register)

| 测试大项 | Level 1 | Level 2 / 模式 | 场景描述 | 测试方法及配置说明 |
| :--- | :--- | :--- | :--- | :--- |
| **MR 功能测试** | **DDR5 模式** | **CTL MRW 测试** | 通过 MC 配置向 MR 寄存器写入数据，再读出校验 | **测试步骤**：<br>1. 阻塞数据事务：拉高 `csrXmuHold`，轮询 `csrXmuIdle` 与 `csrMcIdle` 至高及系统空闲。<br>2. 退出低功耗状态，清除相关 Hold 标志位并判断自刷新退出。<br>3. 置位 `csrSwCmdStart=1`。<br>4. **执行 MRW**：配置 `csrMrrDat`, `csrMrAddr`, `csrMrRank`；设置 **`csrMrType=0x0`**(MRW) 并触发 `csrMrTrig=0x1`，轮询 `csrMrBusy` 变低判断发送成功。<br>5. 恢复：释放 Hold 和低功耗控制，读出比较。 |
| | | **CTL MRR 测试** | 直接读取寄存器配置默认值或读取刚刚写入的数据 | **测试步骤**：<br>前序准备 1~3 步同 MRW。<br>4. **执行 MRR**：配置对应 Rank 和 Addr，设置 **`csrMrType=0x1`**(MRR) 并触发 `csrMrTrig=0x1`，轮询 `csrMrBusy` 变低，读出 `csrMrrDat*` 与预期数据比对。 |
| | | **CTL MPC 测试** | 通过软件 MPC 发送 MPC 命令 (如 RTT_CS等) 并被模型接受 | **测试步骤**：<br>1. 软件配置发送特定的 MPC 序列 (如 `00110xxxB`) 去配置 Group A RTT_CS，等待 MPC 完成。<br>2. 使用上述 MRR 步骤读取 MR32 寄存器，对比 `OP[5:3]` 是否已被成功更改为之前的 MPC 设定值。 |
| | **单个颗粒 MR 测试** | **1N 模式 (PDA 模式)** | 通过 MPC 命令进入 PDA (Per DRAM Addressability) 模式，对单颗颗粒执行 MRW/MRR | **测试流程**：<br>发送相应 MPC 命令开启 PDA。<br>然后只选中对应颗粒完成 MR 操作，比对单个维度的读写是否一致。 |
| | | **2N 模式** | 开启 2N cs timing 模式下执行单个颗粒相关的读写和校验 | **测试流程**：<br>与 1N 下相同，验证改变总线模式对于单颗寻址配置操作的稳定性。 |
| | | | **软件控制字读到 RCD 测试** | 读回 RCD 控制字 | **测试步骤**：与写过程类似，利用特定的命令验证对 RCD 寄存器内的读回通路是否一致。 |
| | | **测试 DMI 功能** | 打开/关闭 DM, DBI 的各类组合 | **验证点**：<br>1. 仅打开 Data Mask (DM)，关闭 DBI进行收发。<br>2. 仅开 Data Bus Inversion (DBI)，关 DM。<br>3. 双开 DBI 与 DM，测试控制器及总线协议栈正确解析屏蔽与翻转功能。 |

---

## 4. 功能混合与并发测试 (Mixed Function Tests)

| 测试大项 | 混合模式 |  | 测试点说明 | 场景描述及测试方法 |
| :--- | :--- | :--- | :--- | :--- |
| **功能混合测试** |  |  |  |  |
|  | **正常读写与 MPC 的混合测试** |  | 控制器执行 MPC 时暂停 DRAM 读写 | **测试方法**：发送 CHI 读写请求的同时触发 MPC 操作。<br>**断言(期望)**：检查 CHI 请求是否被成功阻塞，MPC 完成后是否正常排队发出 CHI 请求。 |
| | **正常读写访问和 MR 的混合测试** |  | MRW/MRR 操作时的事务阻塞验证 | **测试方法**：读写并行插入 MRR/MRW。<br>**期望与断言**：控制器执行该类总线控制时成功堵塞上行请求，结束后数据访问恢复正常。 |
| | **正常读写访问和 BIST 的混合测试** |  | BIST 操作占位验证 | **测试方法**：触发长周期的 BIST 测试同时灌入 CHI 业务流量。<br>**期望与断言**：成功引发数据访问排队，BIST 完成后读写操作成功闭环不丢失。 |
| | **Scrubber 和正常压力读写混合测试** |  | 总线竞争：清洗功能与大压力流量避让 | **测试方法**：配置控制器自动周期性 Scrubber (不强行等待完成位)，并施加大负载读写。<br>**期望与断言**：控制器优先传输 CHI 正常读写访存，总线空闲/出现间隙时再穿插进行 Scrubber 周期间隔发送。 |
| | **Scrubber, umpg 和压力流量综合测试** |  | 多背景巡检任务冲突 | **测试方法**：同时开启周期 Scrubbe 与 umpg 循环测试算法，叠加数据洪峰。<br>**期望结果**：优先级仲裁正确，避免死锁且正常执行所有预定背景功能。 |
| | **LPDDR5 模式** |  |  |  |
| |  | **ECC 混合测试** | **inline ecc 与 link ecc 混合测试** | 同时开启两种 ECC。**测试验证点**：能否正常独立工作并区分出不同物理段/通路的故障，上报准确的中断信号(Type/Syndrome)。 |
| |  | **链路高级组合** | **link ecc 与开启 DBI, DM 混合测试** | 开启 Link 层 ECC，混入数据翻转与屏蔽。<br>**验证点**：组合态下地址校验、MASK 编解码不相互影响总线准确性。 |

---

## 6. SRAM 配置与底座功能测试

| 测试大项             | 测试点                         | 实施方案                                                 | 场景描述与断言验证                                           |
| :------------------- | :----------------------------- | :------------------------------------------------------- | :----------------------------------------------------------- |
| **SRAM config测试**  |                                |                                                          |                                                              |
|                      | **连接性测试**                 | 边带信号旁路控制                                         | **测试方法**：配置用户空间的 `SRAM_CFG_BUS_IN_0/1/2/3` 来调整 SRAM 的边带控制信号 (vr_buffer等五组)。<br>**断言验证**：检查控制器及 PHY 层侧捕获到的端点行为是否与配置对齐生效。 |
|                      |                                |                                                          |                                                              |
| **地址越界错误检测** |                                |                                                          |                                                              |
|                      | **超出 region 空间配置的地址** | 配置 `use_region` 匹配真实容量，发送超出 region 的地址。 | **期望结果**：回环为全0，产生 `addr_out_range_err` 中断，记录越界地址。<br>**测试方法**：(1)与真实容量一致。(2)访问正常地址，查中断=0。(3)访问越界地址，查中断。(4)清中断后再次访问越界，验拉高。 |
|                      | **超出 DRAM 地址**             |                                                          |                                                              |
|                      |                                | `uifaddrmask` 与真实容量配置一致                         | **第一种测试方法(配置=真实容量)**：正常访问后查中为0，越界访问后查 `dbg_addr_out_of_range_err` 中断，清中断再发起越界。<br>**第二种测试方法(配置<真实容量)**：此时超出配置但未超真实的，内部检查中断拉高，同样校验拉高及清掉恢复能力。 |
|                      |                                | `uifaddrmask` 配置为全 F                                 | **测试方法**：(1)配 ba/col/row/cs 范围与真实一致，设 uifmask=F。(2)测正常返回。(3)单次打出超出 DRAM 最大的范围读写，校验地址越界寄存器拉起与留存。(4)清中断并读写。 |
|                      | **超出 bank 地址**             | 跨 Bank 错误检测（依靠内部强压引发）                     | **测试方法**：真实场景难以自发跨错 Bank 返回，故采用 deposit 方式触发强制发生跨 Bank。<br>验证跨 Bank 访问后，跨界中断拉高并记录错误地址；不影响正常访问。(清中断后跨界依然能继续报错)。 |

---

## 7. 高级 ECC 故障注入与恢复 (LPDDR5)

| 特性项 | 注错手段 | 测试序列 (Sequence) | 测试意图与覆盖 |
| :--- | :--- | :--- | :--- |
| **Inline ECC 功能测试**<br />(LPDDR5) |  |  |  |
|  | **硬件注错测试** |  |  |
|  |  | **ECC 1bit / 2bit 错误读写测试** | 通过模型后门/硬件连线破坏 1或2bit，检验自纠正机制或异常抛出以及 Syndrome 的记录准确性。 |
|  | **软件注错测试** |  |  |
| |  | **ECC 1bit / 2bit / 循环注错遍历** | 使用 VIP 提供的软件故障口，不仅测试 1/2bit，并且开启**遍历测试**(循环对不用位置的 Bit 打错)，全覆盖 syndrome 译码表。 |
| **Link ECC 功能测试**<br />(LPDDR5) |  |  |  |
|  | **硬件注错测试** |  |  |
|  |  | **ECC 1bit / 2bit 错误读写测试** | 由于链路 ECC 与 Inline ECC 使用的边界不同，此项专项测控制器内 Link ECC 的纠检能力。 |
|  | **软件注错测试** |  |  |
| |                  | **ECC 1bit / 2bit / 循环注错遍历** | 同样通过 VIP 循环注错，确保链路纠错编码所有分支全部经过遍历。 |
