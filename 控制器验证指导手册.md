# 控制器验证指导手册 (Controller Verification Guide)

本文档旨在基于 IP 数据手册，整理控制器模块的设计理解，并为后续编写测试激励（Test Stimulus）和测试用例（Test Case）提供指导。

---

## 1. 地址映射 (Address Mapping)

### 1.1 映射原理概述

控制器地址映射分为两个阶段进行：

1.  **第一阶段：AXI 地址 -> UIF 地址 (由 XMU 模块处理)**
    *   XMU 会根据 DRAM 命令的数据长度，将 AXI 命令拆分为 UIF 命令。
    *   这个过程中，**有效地址位会被削减**。削减的位数取决于数据长度（Data Length）。
    *   **计算公式**：减少的位数 = `log2(DRAM command data length)`。
    *   **举例**：如果 DRAM 命令数据长度为 64B，则削减 `log2(64) = 6` 位。

2.  **第二阶段：UIF 地址 -> DRAM 地址 (由 IPROC 模块处理)**
    *   IPROC 负责将 UIF 地址映射到具体的 DRAM 物理地址（如 Row, Bank, Column）。
    *   DRAM 地址的每一位都对应一个**可配置寄存器 (CSR)**，该寄存器指定了它映射到 UIF 地址的哪一个 bit 位。

### 1.2 关键寄存器说明 (CSR)

了解这些寄存器对于配置地址交织模式和边界测试至关重要：

| 字段名称 (Field Name) | 位宽/范围 | 默认值 | 描述说明 (Description & Verification Guide) |
| :--- | :--- | :--- | :--- |
| **`Col0Pos[5:0]`** | 0x00 ~ 0x23 | 0x0 | **功能**: 指定列地址的 bit 0 (`Column Address 0`) 映射到 UIF 地址的哪一位。<br>**测试关注点**: `Col0Pos + 6` 就代表它在 AXI 地址中对应的位置。测试时需要覆盖不同 `Col0Pos` 配置下的列地址寻址是否正确。 |
| **`UifAddrMask0[31:0]`** | 0 ~ 0xFFFF_FFFF | 0x0 | **功能**: 指示对应的 UIF 地址的每一位是否有效 (Valid)。为 `1'b1` 表示该位有效，决定实际地址；为 `1'b0` 表示无效，对应的地址位将被固定为 `0`。<br>**测试关注点**: 未使用的 mask 位必须配置为 `1'b0`。测试时需要验证 invalid 状态下地址是否确实被 tie 到了 0。 |
| **`UifAddrMask1[3:0]`** | 0x0 ~ 0xF | 0x0 | **功能**: UIF 地址掩码的高位部分，原理同上。与 `UifAddrMask0` 拼接形成完整的 `UifAddrMask` = `{csrUifAddrMask1, csrUifAddrMask0}`。 |

*(注：例如当 `csrRow16Pos = 6'd28` 时，`uif_addr[28]` 代表 Row Address 16。如果 `UifAddrMask[28] = 1'b1`，则该位有效；如果设为 `0`，则 Row Address 16 强制为 0。)*

---

## 验证激励编写建议 (WIP)

*基于目前获取的手册内容，在编写测试序列（Sequence）时需要考虑：*

1.  **配置阶段 (Configuration Phase)**：
    *   必须能随机或定向配置 `Col0Pos` 等位置寄存器，以模拟不同的地址交织（Interleaving）策略。
    *   必须根据给定的容量/地址空间，正确计算并配置 `UifAddrMask`。
2.  **流量阶段 (Traffic Phase)**：
    *   发出的 AXI 地址需要根据 `UifAddrMask` 和位置寄存器的逆向推导，确保能精准打到预期的物理 Bank 和 Row 上。
    *   **边界测试**：针对掩码为 0 的区域或 `log2(data_length)` 截断的低位区域进行非法/边界地址测试。

---

### 1.3 地址映射测试项提取 (Test Items)

*(注：详细的测点划分及场景描述，请统一参阅独立的 [控制器测试项大纲](控制器测试项大纲.md) 文档。)*
